
resources:
- name: every-10s
  type: time
  icon: clock-outline
  source:
    interval: 10s

- name: after-midnight
  type: time
  source:
    start: 12:00 AM
    stop: 06:00 AM
    location: Asia/Seoul

jobs:

  - name: manual_trigger
    plan:
      - put: every-10s


  - name: triggered-job
    plan:
      - get: every-10s
        trigger: true
        passed: [manual_trigger]
      - get: after-midnight
        trigger: true
      - task: simple-task
        config:
          platform: linux
          image_resource:
            type: registry-image
            source: { repository: busybox }
          run:
            path: echo
            args: ["Hello, world!"]
